# ==========================================
# WEB PROJECT - PRUNES (EXCLUDES)
# ==========================================
WEB_PRUNES=(
    "./node_modules"
    "./dist"
    "./.git"
    "./.vite"
    "./.idea"
    "./.vscode"
    "./coverage"
    ".eslintcache"

    # --- 1. Exclude Images & Media (The "Big Images") ---
    "*.png"
    "*.jpg"
    "*.jpeg"
    "*.gif"
    "*.svg"
    "*.ico"
    "*.webp"
    "*.mp4"
    "*.mov"
    "*.pdf"

    # --- 2. Exclude Test Artifacts (Screenshots/Traces) ---
    # You have a 'test-results' folder in your list, which 
    # usually contains heavy Playwright binaries/images.
    "./test-results"
    "./playwright-report"

    # --- 3. Exclude Infrastructure Binaries ---
    # You have terraform providers (binaries) and state files showing up.
    "./infra/hetzner/.terraform"
    "*.tfstate"
    "*.tfstate.backup"

    # --- 4. Exclude Native Platforms ---
    "./android"
    "./ios"

    # --- 5. Exclude Lock Files and Binaries ---
    # Note: Added 'bun.lock' specifically as it appeared in your list
    "./bun.lock"
    "./bun.lockb"
    "./package-lock.json"
    "./pnpm-lock.yaml"
    "./yarn.lock"
)
